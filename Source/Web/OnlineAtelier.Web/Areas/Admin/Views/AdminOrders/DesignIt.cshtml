@using System.Web.Mvc.Html
@model OnlineAtelier.Web.Models.ViewModels.Order.DesignOrderVm
@{
    ViewBag.Title = "Add";
}

@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()

    <div class="form-horizontal">
        @Html.ValidationSummary(true, "", new { @class = "text-danger" })
        @Html.HiddenFor(model => model.Id)

                @* Category selection dropdown *@
                <div class="form-group">
                    @Html.LabelFor(m => m.FigureName, new { @class = "col-md-2 control-label" })
                    <div class="col-md-6">
                        @Html.DropDownListFor(m => m.FigureName, Model.FiguresSelectList, "- Избери фигура -", new { @class = "form-control" })*
                    </div>
                </div>

        <div class="form-group">
            <div class="col-md-offset-2 col-md-8">
                <input type="submit" class="btn btn-default" value="Добави форма" />
            </div>
        </div>
    </div>
        }
        <div>
            <span class="glyphicon glyphicon-arrow-left" aria-hidden="true"></span>
            @Html.ActionLink("Назад", "Index", "AdminOrders")
        </div>

        @section Scripts {
            @Scripts.Render("~/bundles/jqueryval")
        }